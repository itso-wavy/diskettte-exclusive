export enum DialogMode {
  LOGIN = 'login',
  REGISTER = 'register',
  POST = 'post',
}

export enum FormType {
  CREATE = 'create',
  EDIT = 'edit',
}

export enum PostUIType {
  FEED = 'feed',
  DETAIL = 'detail',
}
